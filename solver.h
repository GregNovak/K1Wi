#ifndef OPUS_OLD_SOLVER_H
#define OPUS_OLD_SOLVER_H

int run_solver(const char *cipherfile,
               const char *quadfile,
               const char *forced_spec,
               int restarts,
               int iterations);

#endif

